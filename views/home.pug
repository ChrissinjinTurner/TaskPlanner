doctype html
html
    head
        
        title #{title}
        link(rel='stylesheet', href='/styelsheets/style.css')

    body
        hi #{title}
        form(action='/', method='post')
          input.ghost-input(name='city', type='text', placeholder='Enter a City', required='')
          input.ghost-button(type='submit', value='Get Weather')
        if weather
          p= weather
        if error
          p= error
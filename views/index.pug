extends layout

block content
    .main.container
        .row
            h1 Task Planner
            form(method='GET' action='/login')
                button.btn.btn-primary(type='submit') Teacher Login
            form(method='GET' action='/studentlogin')
                button.btn.btn-primary(type='submit') Student Login
            form(method='GET' action='/register')
                button.btn.btn-primary(type='submit') Register
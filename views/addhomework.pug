extends layout

block content
    .main.container
        .row
            h1 Add Homework
            form(method='GET' action='/home')
                button.btn.btn-primary(type='submit') Submit
            